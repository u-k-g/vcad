[workspace]
resolver = "2"
members = [
    "crates/vcad-ir",
    "crates/vcad-receipt",
    "crates/vcad-parts",
    "crates/vcad",
    "crates/vcad-kernel-math",
    "crates/vcad-kernel-topo",
    "crates/vcad-kernel-geom",
    "crates/vcad-kernel-primitives",
    "crates/vcad-kernel-naming",
    "crates/vcad-kernel-tessellate",
    "crates/vcad-kernel-booleans",
    "crates/vcad-kernel-nurbs",
    "crates/vcad-kernel-fillet",
    "crates/vcad-kernel-sketch",
    "crates/vcad-kernel-text",
    "crates/vcad-kernel-sweep",
    "crates/vcad-kernel-shell",
    "crates/vcad-kernel-sheet",
    "crates/vcad-kernel-step",
    "crates/vcad-kernel-export",
    "crates/vcad-kernel",
    "crates/vcad-kernel-constraints",
    "crates/vcad-design-constraints",
    "crates/vcad-kernel-diff",
    "crates/vcad-kernel-adjoint",
    "crates/vcad-kernel-antenna",
    "crates/vcad-kernel-neutronics",
    "crates/vcad-kernel-particle",
    "crates/vcad-kernel-optics",
    "crates/vcad-kernel-acoustics",
    "crates/vcad-kernel-em",
    "crates/vcad-kernel-magnetostatic",
    "crates/vcad-kernel-enclosure",
    "crates/vcad-kernel-orbit",
    "crates/vcad-kernel-qcd",
    "crates/vcad-kernel-wasm",
    "crates/vcad-kernel-drafting",
    "crates/vcad-cli",
    "crates/vcad-diff",
    "crates/vcad-kernel-gpu",
    "crates/vcad-kernel-raytrace",
    "crates/vcad-kernel-urdf",
    "crates/vcad-kernel-physics",
    "crates/vcad-kernel-atoms",
    "crates/vcad-render",
    "crates/vcad-typst",
    "crates/wasmosis",
    "crates/wasmosis/wasmosis-macro",
    "crates/vcad-slicer",
    "crates/vcad-slicer-gcode",
    "crates/vcad-slicer-bambu",
    "crates/vcad-kernel-cam",
    "crates/vcad-kernel-stocksim",
    "crates/vcad-kernel-topopt",
    "crates/vcad-kernel-photonics",
    "crates/vcad-kernel-thermal",
    "crates/vcad-kernel-flow",
    "crates/vcad-kernel-fea",
    "crates/vcad-kernel-cost",
    "crates/vcad-kernel-dfm",
    "crates/vcad-kernel-features",
    "crates/vcad-kernel-reconstruct",
    "crates/vcad-kernel-tolerance",
    "crates/vcad-kernel-calibration",
    "crates/vcad-crdt",
    "crates/vcad-app",
    "crates/vcad-chat",
    "crates/vcad-eval",
    "crates/vcad-loon",
    "crates/vcad-sim",
    "crates/vcad-ecad-package",
    "crates/vcad-ecad-parts",
    "crates/vcad-ecad-symbols",
    "crates/vcad-ecad-schematic",
    "crates/vcad-ecad-pcb",
    "crates/vcad-ecad-verify",
    "crates/vcad-ecad-source",
    "crates/vcad-ecad-export",
    "crates/vcad-ecad-fabprep",
    "crates/vcad-ecad-sim",
    "crates/vcad-ecad-diff",
    "crates/vcad-ecad-diff-wasm",
    "crates/vcad-gdsii",
    "crates/vcad-process",
    "crates/stepperoni",
    "crates/termview",
    "crates/vcad-embroidery",
    "crates/vcad-embroidery-pes",
    "crates/vcad-embroidery-dst",
    "crates/vcad-desktop",
    "crates/vcad-ffi",
    "crates/vcad-tool-derive",
    "crates/vcad-i18n",
    "crates/vcad-torture",
    "mecheval/graders",
]

# vcad-desktop pulls in tauri/gtk/atk which requires system dev libraries
# on Linux. Keep it out of the default build set so bare `cargo build`,
# `cargo test`, and `cargo clippy` work on stock dev boxes; CI or whoever
# is actually working on the desktop shell opts in with `-p vcad-desktop`.
default-members = [
    "crates/vcad-ir",
    "crates/vcad-receipt",
    "crates/vcad-parts",
    "crates/vcad",
    "crates/vcad-kernel-math",
    "crates/vcad-kernel-topo",
    "crates/vcad-kernel-geom",
    "crates/vcad-kernel-primitives",
    "crates/vcad-kernel-naming",
    "crates/vcad-kernel-tessellate",
    "crates/vcad-kernel-booleans",
    "crates/vcad-kernel-nurbs",
    "crates/vcad-kernel-fillet",
    "crates/vcad-kernel-sketch",
    "crates/vcad-kernel-text",
    "crates/vcad-kernel-sweep",
    "crates/vcad-kernel-shell",
    "crates/vcad-kernel-sheet",
    "crates/vcad-kernel-step",
    "crates/vcad-kernel-export",
    "crates/vcad-kernel",
    "crates/vcad-kernel-constraints",
    "crates/vcad-design-constraints",
    "crates/vcad-kernel-diff",
    "crates/vcad-kernel-adjoint",
    "crates/vcad-kernel-antenna",
    "crates/vcad-kernel-neutronics",
    "crates/vcad-kernel-particle",
    "crates/vcad-kernel-optics",
    "crates/vcad-kernel-acoustics",
    "crates/vcad-kernel-em",
    "crates/vcad-kernel-magnetostatic",
    "crates/vcad-kernel-enclosure",
    "crates/vcad-kernel-orbit",
    "crates/vcad-kernel-qcd",
    "crates/vcad-kernel-wasm",
    "crates/vcad-kernel-drafting",
    "crates/vcad-cli",
    "crates/vcad-diff",
    "crates/vcad-kernel-gpu",
    "crates/vcad-kernel-raytrace",
    "crates/vcad-kernel-urdf",
    "crates/vcad-kernel-physics",
    "crates/vcad-kernel-atoms",
    "crates/vcad-render",
    "crates/wasmosis",
    "crates/wasmosis/wasmosis-macro",
    "crates/vcad-slicer",
    "crates/vcad-slicer-gcode",
    "crates/vcad-slicer-bambu",
    "crates/vcad-kernel-cam",
    "crates/vcad-kernel-stocksim",
    "crates/vcad-kernel-topopt",
    "crates/vcad-kernel-photonics",
    "crates/vcad-kernel-thermal",
    "crates/vcad-kernel-flow",
    "crates/vcad-kernel-fea",
    "crates/vcad-kernel-cost",
    "crates/vcad-kernel-dfm",
    "crates/vcad-kernel-features",
    "crates/vcad-kernel-reconstruct",
    "crates/vcad-kernel-tolerance",
    "crates/vcad-kernel-calibration",
    "crates/vcad-crdt",
    "crates/vcad-app",
    "crates/vcad-chat",
    "crates/vcad-eval",
    "crates/vcad-loon",
    "crates/vcad-sim",
    "crates/vcad-ecad-package",
    "crates/vcad-ecad-parts",
    "crates/vcad-ecad-symbols",
    "crates/vcad-ecad-schematic",
    "crates/vcad-ecad-pcb",
    "crates/vcad-ecad-verify",
    "crates/vcad-ecad-source",
    "crates/vcad-ecad-export",
    "crates/vcad-ecad-fabprep",
    "crates/vcad-ecad-sim",
    "crates/vcad-ecad-diff",
    "crates/vcad-ecad-diff-wasm",
    "crates/vcad-gdsii",
    "crates/vcad-process",
    "crates/stepperoni",
    "crates/termview",
    "crates/vcad-embroidery",
    "crates/vcad-embroidery-pes",
    "crates/vcad-embroidery-dst",
    "crates/vcad-ffi",
    "crates/vcad-tool-derive",
    "crates/vcad-i18n",
    "crates/vcad-torture",
    "mecheval/graders",
]

[workspace.package]
version = "0.9.4"
edition = "2021"
license = "MIT"
repository = "https://github.com/ecto/vcad"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
# Path-only deps from sibling repos go through path locally; the version is
# what cargo publish embeds into the registry manifest. Bump these when the
# sibling repo cuts a release; sync-version.mjs does NOT touch them.
tang = { path = "../tang/crates/tang", version = "0.2" }
tang-la = { path = "../tang/crates/tang-la", version = "0.1" }
tang-expr = { path = "../tang/crates/tang-expr", version = "0.1" }
# phyz is PINNED to a published revision rather than a sibling checkout.
#
# As a path dep, every vcad build took whatever happened to be in
# `../phyz` — including a colleague's half-finished edit. That is not a
# hypothetical: a working tree mid-refactor (a changed `contact::assemble`
# signature, a `manifold::Face` missing a field) blocked every
# physics-dependent build and test on this machine for an afternoon, and the
# breakage had nothing to do with vcad. CI never saw it, because CI clones
# phyz fresh — so the two disagreed about what "the build" even meant.
#
# Pinning makes the dependency reproducible and the sibling checkout
# irrelevant. Bump the rev deliberately, in its own commit, so a physics
# change to vcad is always attributable.
#
# phyz moved every crate onto its workspace version (0.3.1); the sub-crates
# are no longer independently 0.1.x.
phyz = { git = "https://github.com/ecto/phyz.git", rev = "7c8ac59c7659ed64f7218e0504b0487f90ec68ef", version = "0.3" }
phyz-gpu = { git = "https://github.com/ecto/phyz.git", rev = "7c8ac59c7659ed64f7218e0504b0487f90ec68ef", version = "0.3" }
phyz-model = { git = "https://github.com/ecto/phyz.git", rev = "7c8ac59c7659ed64f7218e0504b0487f90ec68ef", version = "0.3" }
phyz-math = { git = "https://github.com/ecto/phyz.git", rev = "7c8ac59c7659ed64f7218e0504b0487f90ec68ef", version = "0.3" }
phyz-md = { git = "https://github.com/ecto/phyz.git", rev = "7c8ac59c7659ed64f7218e0504b0487f90ec68ef", version = "0.3" }
slotmap = "1"
anyhow = "1"
toml = "0.8"
gltf = { version = "1.4" }
base64 = { version = "0.22" }
opencascade = { version = "0.2" }
# Intra-workspace deps. Versions are kept lockstep with workspace.package.version
# by scripts/sync-version.mjs — don't hand-edit them, edit a changelog entry and
# run `npm run version:sync`.
vcad-ir = { path = "crates/vcad-ir", version = "0.9.4" }
vcad-receipt = { path = "crates/vcad-receipt", version = "0.9.4" }
vcad-parts = { path = "crates/vcad-parts", version = "0.9.4" }
vcad-kernel = { path = "crates/vcad-kernel", version = "0.9.4" }
vcad-kernel-math = { path = "crates/vcad-kernel-math", version = "0.9.4" }
vcad-kernel-topo = { path = "crates/vcad-kernel-topo", version = "0.9.4" }
vcad-kernel-geom = { path = "crates/vcad-kernel-geom", version = "0.9.4" }
vcad-kernel-primitives = { path = "crates/vcad-kernel-primitives", version = "0.9.4" }
vcad-kernel-naming = { path = "crates/vcad-kernel-naming", version = "0.9.4" }
vcad-kernel-tessellate = { path = "crates/vcad-kernel-tessellate", version = "0.9.4" }
vcad-kernel-booleans = { path = "crates/vcad-kernel-booleans", version = "0.9.4" }
vcad-kernel-nurbs = { path = "crates/vcad-kernel-nurbs", version = "0.9.4" }
vcad-kernel-fillet = { path = "crates/vcad-kernel-fillet", version = "0.9.4" }
vcad-kernel-sketch = { path = "crates/vcad-kernel-sketch", version = "0.9.4" }
vcad-kernel-sweep = { path = "crates/vcad-kernel-sweep", version = "0.9.4" }
vcad-kernel-shell = { path = "crates/vcad-kernel-shell", version = "0.9.4" }
vcad-kernel-sheet = { path = "crates/vcad-kernel-sheet", version = "0.9.4" }
vcad-kernel-step = { path = "crates/vcad-kernel-step", version = "0.9.4" }
vcad-kernel-export = { path = "crates/vcad-kernel-export", version = "0.9.4" }
vcad-kernel-constraints = { path = "crates/vcad-kernel-constraints", version = "0.9.4" }
vcad-design-constraints = { path = "crates/vcad-design-constraints", version = "0.9.4" }
vcad-kernel-diff = { path = "crates/vcad-kernel-diff", version = "0.9.4" }
vcad-kernel-adjoint = { path = "crates/vcad-kernel-adjoint", version = "0.9.4" }
vcad-kernel-antenna = { path = "crates/vcad-kernel-antenna", version = "0.9.4" }
vcad-kernel-neutronics = { path = "crates/vcad-kernel-neutronics", version = "0.9.4" }
vcad-kernel-particle = { path = "crates/vcad-kernel-particle", version = "0.9.4" }
vcad-kernel-optics = { path = "crates/vcad-kernel-optics", version = "0.9.4" }
vcad-kernel-acoustics = { path = "crates/vcad-kernel-acoustics", version = "0.9.4" }
vcad-kernel-em = { path = "crates/vcad-kernel-em", version = "0.9.4" }
vcad-kernel-magnetostatic = { path = "crates/vcad-kernel-magnetostatic", version = "0.9.4" }
vcad-kernel-enclosure = { path = "crates/vcad-kernel-enclosure", version = "0.9.4" }
vcad-kernel-orbit = { path = "crates/vcad-kernel-orbit", version = "0.9.4" }
vcad-kernel-qcd = { path = "crates/vcad-kernel-qcd", version = "0.9.4" }
vcad-kernel-drafting = { path = "crates/vcad-kernel-drafting", version = "0.9.4" }
vcad-kernel-gpu = { path = "crates/vcad-kernel-gpu", version = "0.9.4" }
vcad-kernel-raytrace = { path = "crates/vcad-kernel-raytrace", version = "0.9.4" }
vcad-kernel-urdf = { path = "crates/vcad-kernel-urdf", version = "0.9.4" }
vcad-kernel-physics = { path = "crates/vcad-kernel-physics", version = "0.9.4" }
vcad-kernel-atoms = { path = "crates/vcad-kernel-atoms", version = "0.9.4" }
wasmosis = { path = "crates/wasmosis", version = "0.9.4" }
wasmosis-macro = { path = "crates/wasmosis/wasmosis-macro", version = "0.9.4" }
vcad-slicer = { path = "crates/vcad-slicer", version = "0.9.4" }
vcad-kernel-cost = { path = "crates/vcad-kernel-cost", version = "0.9.4" }
vcad-kernel-dfm = { path = "crates/vcad-kernel-dfm", version = "0.9.4" }
vcad-kernel-features = { path = "crates/vcad-kernel-features", version = "0.9.4" }
vcad-kernel-reconstruct = { path = "crates/vcad-kernel-reconstruct", version = "0.9.4" }
vcad-kernel-tolerance = { path = "crates/vcad-kernel-tolerance", version = "0.9.4" }
vcad-kernel-calibration = { path = "crates/vcad-kernel-calibration", version = "0.9.4" }
vcad-slicer-gcode = { path = "crates/vcad-slicer-gcode", version = "0.9.4" }
vcad-slicer-bambu = { path = "crates/vcad-slicer-bambu", version = "0.9.4" }
vcad-kernel-cam = { path = "crates/vcad-kernel-cam", version = "0.9.4" }
vcad-kernel-stocksim = { path = "crates/vcad-kernel-stocksim", version = "0.9.4" }
vcad-kernel-topopt = { path = "crates/vcad-kernel-topopt", version = "0.9.4" }
vcad-kernel-photonics = { path = "crates/vcad-kernel-photonics", version = "0.9.4" }
vcad-kernel-thermal = { path = "crates/vcad-kernel-thermal", version = "0.9.4" }
vcad-kernel-flow = { path = "crates/vcad-kernel-flow", version = "0.9.4" }
vcad-kernel-fea = { path = "crates/vcad-kernel-fea", version = "0.9.4" }
vcad-kernel-text = { path = "crates/vcad-kernel-text", version = "0.9.4" }
vcad-crdt = { path = "crates/vcad-crdt", version = "0.9.4" }
vcad-app = { path = "crates/vcad-app", version = "0.9.4" }
vcad-eval = { path = "crates/vcad-eval", version = "0.9.4" }
vcad-loon = { path = "crates/vcad-loon", version = "0.9.4" }
vcad-sim = { path = "crates/vcad-sim", version = "0.9.4" }
vcad-ecad-package = { path = "crates/vcad-ecad-package", version = "0.9.4" }
vcad-ecad-parts = { path = "crates/vcad-ecad-parts", version = "0.9.4" }
vcad-ecad-symbols = { path = "crates/vcad-ecad-symbols", version = "0.9.4" }
vcad-ecad-schematic = { path = "crates/vcad-ecad-schematic", version = "0.9.4" }
vcad-ecad-pcb = { path = "crates/vcad-ecad-pcb", version = "0.9.4" }
vcad-ecad-verify = { path = "crates/vcad-ecad-verify", version = "0.9.4" }
vcad-ecad-source = { path = "crates/vcad-ecad-source", version = "0.9.4" }
vcad-ecad-export = { path = "crates/vcad-ecad-export", version = "0.9.4" }
vcad-ecad-fabprep = { path = "crates/vcad-ecad-fabprep", version = "0.9.4" }
vcad-ecad-sim = { path = "crates/vcad-ecad-sim", version = "0.9.4" }
vcad-ecad-diff = { path = "crates/vcad-ecad-diff", version = "0.9.4" }
nom = "7"
rstar = "0.12"
stepperoni = { path = "crates/stepperoni", version = "0.1" }
termview = { path = "crates/termview", version = "0.9.4" }
vcad-embroidery = { path = "crates/vcad-embroidery", version = "0.9.4" }
vcad-embroidery-pes = { path = "crates/vcad-embroidery-pes", version = "0.9.4" }
vcad-embroidery-dst = { path = "crates/vcad-embroidery-dst", version = "0.9.4" }
vcad-tool-derive = { path = "crates/vcad-tool-derive", version = "0.9.4" }
vcad-gdsii = { path = "crates/vcad-gdsii", version = "0.9.4" }
vcad-process = { path = "crates/vcad-process", version = "0.9.4" }

# GPU stack — version-pin here, each consumer opts into the features it needs.
wgpu = { version = "23" }
bytemuck = { version = "1.14", features = ["derive"] }
pollster = "0.4"

# Unwinding panics let the evaluator catch a kernel assertion (e.g. the
# add_loop invariant in vcad-kernel-topo) and turn it into a per-root
# failure instead of aborting the whole WASM module with an `unreachable`
# trap. The ~small binary-size cost is worth keeping 19 good features
# rendering when a 20th hits a kernel bug.
[profile.release]
panic = "unwind"

[profile.dev]
panic = "unwind"
# The kernel is numerics-heavy and its tests run in this profile. Unoptimized
# they are ~20x slower: the torr boolean catalogue alone took 20.8 min of the
# stable Rust CI job's 25.4 min `cargo test` step at opt-level 0, and 7.9s at
# opt-level 2 (same 24 tests, same machine). `debug_assertions` and overflow
# checks stay on, unlike `--release` — see the vcad-kernel-fea override below,
# which is the same trick applied to one crate before this was workspace-wide.
opt-level = 2

# Size-optimized profile for the Typst plugin WASM
# (scripts/build-typst-plugin.sh).
[profile.typst-wasm]
inherits = "release"
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

# The FEA refinement studies run PCG on ~100k-DOF systems in their tests;
# unoptimized they take minutes, optimized seconds.
[profile.dev.package.vcad-kernel-fea]
opt-level = 3

# The sibling repos (phyz, tang) depend on each other through crates.io
# versions, and phyz redirects tang to its own sibling checkout with a
# `[patch.crates-io]` of its own. Cargo only honours the patch table of the
# workspace ROOT being built, so when vcad builds phyz as a path dependency
# phyz's patch is ignored and its `tang-expr = "0.1.0"` resolves from the
# registry — while vcad's own tang comes from `../tang`. Two tangs in one
# graph, and the published tang-expr is older than the tang it must satisfy:
#
#   error[E0277]: the trait bound `ExprId: Scalar` is not satisfied
#     --> phyz/crates/phyz-diff/src/symbolic.rs:234
#
# Mirroring phyz's patch here unifies every tang reference in the graph on
# the sibling checkout, which is the source of truth for both repos anyway.
[patch.crates-io]
tang = { path = "../tang/crates/tang" }
tang-la = { path = "../tang/crates/tang-la" }
tang-expr = { path = "../tang/crates/tang-expr" }
# clipper-sys 0.7.2's build.rs panics on tier-3 Apple targets (visionOS); the
# vendored copy adds the missing libc++ link arms. Identical source otherwise.
clipper-sys = { path = "third_party/clipper-sys" }
